---
// Root index — renders whichever theme is currently preferred.
// To switch the active theme, update ACTIVE_THEME below (e.g. '/1', '/2', '/3').
// When the ThemeSwitcher + Cloudflare Worker are wired up, this file goes away
// and the Worker handles routing at the edge instead.

return Astro.rewrite('/2'); // change this to '/1', '/3', etc. to switch active theme
---
